{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "base",
  "description": "The base properties used in all top-level types",
  "type": "object",
  "properties": {
    "_type": {
      "type": "string",
      "description": "Hardcoded value to indicate what type of construct this is"
    },
    "_id": {
      "type": "string",
      "description": "Globally Unique Identifier for this construct. MUST be a UUIDv4."
    },
    "urls": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Time at which this construct was created. MUST be a UTC time."
    },
    "credibility": {
      "type": "integer",
      "minimum": 0,
      "maximum": 6,
      "description": "The credibility of this relationship, using the Admirality scale"
    },
    "external_ids": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "source": {
            "type": "string",
            "description": "The source of this ID, i.e. name of an external system"
          },
          "id": {
            "type": "string",
            "description": "ID itself"
          }
        }
      }
    },
    "producer": {
      "$ref": "identity.json"
    },
    "marking_ids": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["_type", "_id", "timestamp", "credibility"]
}
